import { App, Plugin } from 'obsidian';

export default class UserStatsPlugin extends Plugin {
	async onload() {
		// there's nothing to see here yet, will plan onto adding some things as soon as possible ;)
	}
}
